/** @type {import('next').NextConfig} */
const nextConfig = {
    optimizeFonts: true,
    optimizeImages: true,
    optimizeCss: true,
    runtime: 'experimental-edge',
};

export default nextConfig;
